tools:
  api_post:
    description: POST JSON payload to a REST endpoint
    input_schema:
      endpoint: string
      payload: object
  db_query:
    description: Execute a SQL query against the configured database
    input_schema:
      query: string
  file_read:
    description: Read a file from disk
    input_schema:
      path: string
